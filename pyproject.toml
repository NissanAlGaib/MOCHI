[project]
name = "mochi"
version = "0.1.0"
description = "Middleware for Observing, Classifying, and Handling Prompt Injections"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
filterwarnings = ["ignore::DeprecationWarning"]
