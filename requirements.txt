# MOCHI core runtime (Phase 0-1)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# Phase 3 - normalization / de-obfuscation
beautifulsoup4>=4.12.0     # HTML hidden-channel extraction
pdfplumber>=0.11.0         # PDF text + metadata extraction

# Testing & evidence reporting
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0          # coverage report
pytest-html>=4.1.0         # standalone HTML report for the thesis appendix

# Phase 8 - Stage II semantic detection.
#
# Deliberately NOT installed by default. Stage II is off unless
# MOCHI_ENABLE_STAGE2=true, torch is ~2.5 GB, and every code path in
# mochi/detect/stage2_semantic.py is tested through a stub scorer - so a
# developer evaluating MOCHI, or deploying it with Stage I only, should not pay
# for a deep-learning stack they will not load. Install when training or when
# running with a trained model:
#
#   pip install torch transformers
#
# torch>=2.4.0
# transformers>=4.45.0
# sentence-transformers is NOT used: its default mean pooling is the behaviour
# Stage II specifically avoids (see training/model.py). The encoder is loaded
# through transformers directly so the pooling layer is ours.

# Added in later phases (kept commented so the install stays light):
# openai>=1.50.0             # Phase 9 - Stage III arbiter
# scikit-learn>=1.5.0        # Phase 5/13 - evaluation metrics
# pandas>=2.2.0              # Phase 5/13 - dataset handling
# scipy>=1.14.0              # Phase 13 - paired t-test, Cohen's d
